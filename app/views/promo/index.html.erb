<div class="promos-container">
    <% if @promos.any? %>
      <% @promos.each do |promo| %>
        <div class="promo">
          <img src=<%= url_for(promo.image)%>></img>
          <%= link_to promo.name.titleize, promo_path(promo) %>
        </div>
      <% end %>
    <% end %>
</div>
