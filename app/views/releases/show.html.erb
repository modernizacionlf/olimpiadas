<div class="header-realese">
<% if @release.image.attached? %>
  <img src=<%= url_for(@release.image) %>></img>
<% end %>
<h1><%= @release.title.titleize %></h1>
</div>
<div class="body-realese">
  <p><%= @release.description.capitalize %></p>
</div>
