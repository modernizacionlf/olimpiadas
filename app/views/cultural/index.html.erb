<div class="-culture-container">
  <% if @culturals.any? %>
    <% @culturals.each do |cultural| %>
      <div class="sport">
        <%= image_tag cultural.image %>
        <h2><%=link_to cultural.title.titleize, cultural_path(cultural) %></h2>
      </div>
    <% end %>
  <% end %>
</div>
