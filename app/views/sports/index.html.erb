<div class="sports-container">
  <% if @sports.any? %>
    <% @sports.each do |sport| %>
      <div class="sport">
        <h2><%=link_to sport.name.titleize, sport_path(sport) %></h2>
      </div>
    <% end %>
  <% end %>
</div>
