<div class="sports-container">
  <% if @sports.any? %>
    <% @sports.each do |sport| %>
      <div class="sport">
        <img><%= link_to "TODO", sport_path(sport) %></img>
        <h2><%= sport.name.titleize %></h2>
      </div>
    <% end %>
  <% end %>
</div>
